.\" Man page for scd-post-entry
.TH SCD\-POST\-ENTRY 1 "2026-06-02" "SCD Effort Reporting" "SCD Effort Reporting Commands"
.
.SH NAME
scd\-post\-entry \- interactively create a work entry via the SCD Activity Reporting API
.
.SH SYNOPSIS
.B scd\-post\-entry
.RB [ \-c
.IR file ]
.RB [ \-u
.IR url ]
.RB [ \-t
.IR token ]
.
.SH DESCRIPTION
.B scd\-post\-entry
is an interactive command\-line utility that guides you through creating a
new work entry in the SCD Activity Reporting web application.  It submits
the entry via the application's REST API endpoint
.RI ( POST\ /api/entries/ ).
.PP
The tool prompts for all required and optional fields, then opens
.BR $EDITOR
for the multi\-line entry description.  A summary is displayed before
submission, and confirmation is required before the request is sent.
.PP
Authentication uses a personal API token issued by the web application.
No password or session cookie is required.  See
.B TOKEN SETUP
below.
.
.SH OPTIONS
.TP
.BI \-c\  file ,\ \-\-config\  file
Path to the YAML configuration file.
.br
Default:
.I ~/.config/scd\-reporting/config.yaml
.TP
.BI \-u\  url ,\ \-\-url\  url
Base URL of the SCD Reporting instance,
e.g.\&
.IR https://scd\-reporting.fnal.gov .
Overrides the
.B url
config key and the
.B SCD_URL
environment variable.
.TP
.BI \-t\  token ,\ \-\-token\  token
API token.
Overrides the
.B token
config key and the
.B SCD_TOKEN
environment variable.
Avoid passing the token on the command line on shared systems; use the
config file or environment variable instead.
.
.SH TOKEN SETUP
API tokens are generated in the web application under
.BR "Profile > API Token" .
Each user has at most one active token; generating a new token immediately
invalidates the previous one.
.PP
Once you have a token, add it to the configuration file:
.PP
.nf
    url:   https://scd\-reporting.fnal.gov
    token: <paste token here>
.fi
.PP
Alternatively, export it as an environment variable:
.PP
.nf
    export SCD_TOKEN=<paste token here>
.fi
.PP
The token is a 64\-character hexadecimal string.  Keep it secret — anyone
who holds it can post entries on your behalf.  If a token is compromised,
use
.B Revoke
on the Profile page to invalidate it immediately, then generate a new one.
.
.SH FIELDS
The tool prompts for the following fields in order:
.TP
.B Title
Short summary of the work item.
Required.
.TP
.B Project
Project slug or numeric ID (e.g.\&
.IR mu2e\-daq ).
Required.
See
.I /taxonomy/projects/
in the web UI for available slugs.
.TP
.B Category
Category slug or numeric ID.
Required.
See
.I /taxonomy/categories/
in the web UI.
.TP
.B Period kind
One of:
.BR today ,
.BR week ,
.BR fortnight ,
.BR month ,
.BR custom .
Default:
.BR week .
.TP
.B Period start / Period end
Reporting period dates in
.B YYYY\-MM\-DD
format.
Defaults are computed automatically from the chosen period kind
(e.g.\& the current calendar week for
.BR week ).
.TP
.B Group
Work group slug or ID.
Optional.
.TP
.B Lab priority
Lab priority slug or ID.
Optional.
.TP
.B Tags
Comma\-separated list of tag names.
Optional.
Tags that do not exist are created automatically.
.TP
.B Flags
.RS
.TP
.B Private
Hides the entry from most report views.
.TP
.B Critical
Marks the entry as critical work.
.TP
.B Highlight
Marks the entry as a highlight.  A star rating (0\(en5) is requested
when this flag is set.
.TP
.B Division head only
Restricts visibility to division heads and administrators.
.RE
.TP
.B Description
Free\-form Markdown text describing the work.
The
.B $EDITOR
(or
.BR $VISUAL )
environment variable controls which editor is launched.
Falls back to
.B vi
on Unix and
.B notepad
on Windows.
The editor opens with a standard template pre\-filled.
.
.SH CONFIGURATION FILE
The default configuration file is
.IR ~/.config/scd\-reporting/config.yaml .
Example:
.PP
.nf
    url:   https://scd\-reporting.fnal.gov
    token: 3a9f2c...  # 64-char hex token from Profile
.fi
.PP
Settings are resolved in this order (highest priority first):
command\-line flags, environment variables, config file, interactive prompt.
.
.SH ENVIRONMENT
.TP
.B SCD_URL
Base URL of the SCD Reporting instance.
Overridden by
.BR \-\-url .
.TP
.B SCD_TOKEN
API token.
Overridden by
.BR \-\-token .
.TP
.B EDITOR
Editor launched for the description field.
.TP
.B VISUAL
Preferred editor; checked before
.BR EDITOR .
.
.SH EXIT STATUS
.TP
.B 0
Entry created successfully.
.TP
.B 1
Validation error, authentication failure, or network error.
The specific message is printed to stderr.
.
.SH EXAMPLES
.PP
Run with all settings in the config file:
.PP
.nf
    scd\-post\-entry
.fi
.PP
Override the URL for a dev instance:
.PP
.nf
    scd\-post\-entry \-u http://localhost:8000
.fi
.PP
Pass the token inline (not recommended on shared systems):
.PP
.nf
    scd\-post\-entry \-\-token 3a9f2c...
.fi
.PP
Use a non\-default config file:
.PP
.nf
    scd\-post\-entry \-c ~/my\-scd\-config.yaml
.fi
.PP
Set the editor to nano for the description:
.PP
.nf
    EDITOR=nano scd\-post\-entry
.fi
.
.SH FILES
.TP
.I scripts/scd\-post\-entry
Source file for this utility.
.TP
.I ~/.config/scd\-reporting/config.yaml
User configuration file.  Contains
.B url
and
.B token
keys.
.
.SH SEE ALSO
.BR scd\-seed\-admin (1),
.BR scd\-seed\-taxonomy (1),
.BR vi (1),
.BR curl (1)
.PP
SCD Activity Reporting API documentation:
.I /api/
.PP
Token management:
.I /profile/
.
.SH AUTHOR
Fermilab Scientific Computing Division
